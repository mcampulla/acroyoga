@model int
@{
    Layout = "~/Views/Shared/_LayoutInner.cshtml";
}
<script>
    var index = @(Model);
</script>
<div class="overlay"><div><i class="fa fa-circle-o-notch fa-spin" style="font-size:48px"></i><br />Caricamento...</div></div>
<div class="spacer-60"></div>
<section id="team" class="wow fadeIn ptb ptb-sm-80">
    <div class="container" ng-app="Event" ng-controller="EventController">
        <div class="row" ng-cloak>
            <div class="col-md-12">
                <div class="row">
                    <div class="col-md-4">
                        <img ng-src="{{getImageUrl(currentEvent.ImageLeft)}}" alt="" class="img-responsive" />
                    </div>
                    <div class="col-md-8">
                        <h3>{{currentEvent.Title}}</h3>
                        <p ng-bind-html="body"></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
